{
  "name": "aura-health-telehealth",
  "version": "1.0.0",
  "scripts": {
    "start": "node start.js",
    "build": "cd frontend && npm install && npm run build && cd ../telehealth-backend && npm install",
    "install-backend": "npm install --prefix telehealth-backend",
    "install-frontend": "npm install --prefix frontend",
    "build-frontend": "npm run build --prefix frontend"
  },
  "dependencies": {
    "twilio": "^6.0.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
